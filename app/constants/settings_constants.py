# if you change the path of the env files, change these values also
envOptionsPaths = {
            'test': 'stage.test.env',
            'prod': 'stage.production.env',
            'dev': 'stage.development.env'
        }
